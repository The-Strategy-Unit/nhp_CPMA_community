This analysis is to explore how potentially mitigable activity for the `r cohort_title` `r cohort_group` varies by Integrated Care Board and Local Authority.

### Integrated Care Board

```{r}
#| output: asis
knitr::knit_child(
                  input = "child-dir/_child-comparative_icb.qmd",
                  envir = environment(),
                  quiet = TRUE
                ) |>
                  cat(sep = '\n')
```

### Local Authority

```{r}
#| output: asis
geography <- "la"

knitr::knit_child(
                  input = "child-dir/_child-comparative_local-authority.qmd",
                  envir = environment(),
                  quiet = TRUE
                ) |>
                  cat(sep = '\n')
```

### Acute Provider

```{r}
#| output: asis
geography <- "provider"

knitr::knit_child(
                  input = "child-dir/_child-comparative_local-authority.qmd",
                  envir = environment(),
                  quiet = TRUE
                ) |>
                  cat(sep = '\n')
```